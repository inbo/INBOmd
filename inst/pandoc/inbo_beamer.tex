% de presentatie
\documentclass[
  hyperref={colorlinks=true,citecolor=link.colour,linkcolor=link.colour,urlcolor=link.colour},
  $if(fontsize)$$fontsize$$else$10pt$endif$,
  xetex,
  noamsthm$if(handout)$, handout$endif$$if(aspect)$, aspectratio=$aspect$ $endif$
]{beamer}

\usetheme{$theme$}
\usepackage{inbogeneric_2015}
$if(codesize)$
\codesize{\$codesize$}
$else$
\codesize{\footnotesize}
$endif$
$if(website)$
\website{$website$}
$else$
\website{www.vlaanderen.be/inbo}
$endif$
$if(flandersfont)$
\flandersfont{TRUE}
$endif$
\usetheme{vlaanderenfont}

$if(mylanguage)$
\lang{$mylanguage$}
$endif$
$if(cover_photo)$
\cover{$cover_photo$}
$endif$
$if(cover_horizontal)$
\coverhorizontal{$cover_horizontal$}
$endif$

$if(cover_offset)$
\coveroffset{$cover_offset$}
$endif$
$if(cover_hoffset)$
\coverhoffset{$cover_hoffset$}
$endif$
%title verschijnt op de eerste slide. subject verschijnt enkel in de
% eigenschappen van de pdf
\title{$title$}

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

\author{
$for(author)$$if(author.name)$$if(author.firstname)$$author.firstname$ $endif$$author.name$$else$$author$$endif$$sep$, $endfor$
}

$if(institute)$
\institute{$institute$}
$endif$

$if(location)$
\date{$location$}
$else$
\date{}
$endif$

$if(slide_logo)$
\slidelogo{$slide_logo$}
$endif$

\AtBeginSection[]
{
  \begin{frame}[plain]
    \sectionpage
  \end{frame}
}
\AtBeginSubsection[]
{
  \begin{frame}[plain]
    \subsectionpage
  \end{frame}
}

\makeatletter
\renewcommand{\verbatim@font}{\ttfamily\$if(codesize)$$codesize$$else$footnotesize$endif$}
\makeatother

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}

\setbeamertemplate{firstpage}{}
\begin{frame}[plain, label=intro, noframenumbering]
  \titlepage
\end{frame}

\setbeamertemplate{firstpage}{no}
$for(include-before)$
$include-before$

$endfor$

$if(toc)$
\begin{frame}
$if(toc_name)$
  \frametitle{$toc_name$}
$else$
  \frametitle{Overzicht}
$endif$
	\tableofcontents
\end{frame}
$endif$

$body$

$for(include-after)$
$include-after$

$endfor$
\end{document}
